include_rules

SRC += util.c
SRC += luax.c
SRC += lovr.c
SRC += main.c

API += api/lovr.c

RES += resources/boot.lua.h

: foreach $(API) |> !cc_api |>
: foreach $(SRC) | $(RES) |> !cc $(CFLAGS_lua) $(CFLAGS_glfw) |> $(ROOT)/obj/lovr_%B.o $(ROOT)/<obj_exe>
